@model List<EpisodeViewModel>
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Episodes</h4>
                <a asp-controller="episode" asp-action="create" class="btn btn-primary">Create episode</a>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Image</th>
                                <th>Title</th>
                                <th>Views</th>
                                <th>Speaker</th>
                                <th>Topic</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td><img src="@item.CoverUrl" /></td>
                                    <td>@item.Title</td>
                                    <td>@item.ViewCount</td>
                                    <td>@item.Speaker?.Name</td>
                                    <td>@item.Topic?.Name</td>
                                    <td>
                                        <a class="btn btn-primary" asp-controller="episode" asp-action="details" asp-route-id="@item.Id">Details</a>
                                        <a class="btn btn-warning" asp-controller="episode" asp-action="update" asp-route-id="@item.Id">Update</a>
                                        <a class="btn btn-danger" asp-controller="episode" asp-action="delete" asp-route-id="@item.Id">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>